import socket
from sss import SSS

s = socket.socket()
port = 12345
s.connect(('localhost',port))
shares = []
while True:
    if 
    shares.append(tuple(str(s.recv(1024),encoding='ascii').split(',')))
    try: 
       sss = SSS()

s.close()
